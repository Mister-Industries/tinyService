# TinyService Test Interface

## Summary

Typescript + Vite application to provide a visual way to test the arduino service independently of the full IDE ui. Uses the shadcn design system for styling
